La propiedad `position` en CSS define **cómo se posiciona un elemento** en el documento y **respecto a qué referencia se posiciona**.

---
## Resumen de propiedades

| position | effect                                      |
| -------- | ------------------------------------------- |
| relative | positioned relative to where it normally is |
| fixed    | positioned relative to the viewport         |
| absolute | positioned relative to nearest ancestor     |
| sticky   | positioned based on scroll position         |
| static   | default position for an element             |

---

## 📌 Valores posibles

### 1. `static` (por defecto)

- El elemento sigue el **flujo normal** del documento.

- Ignora las propiedades `top`, `right`, `bottom`, `left`.


```css
.elemento {
  position: static;
}
```

---

### 2. `relative`

- El elemento **mantiene su lugar original** en el flujo.

- Se **mueve respecto a sí mismo** usando `top`, `left`, `right`, `bottom`.

- Deja el espacio original ocupado.


```css
.elemento {
  position: relative;
  top: 10px;
  left: 20px;
}
```

🔹 Muy útil para crear pequeños ajustes o ser **referencia de elementos `absolute` hijos**.

---

### 3. `absolute`

- El elemento **sale del flujo del documento**.

- Se posiciona **respecto al contenedor más cercano con `position: relative`, `absolute` o `fixed`**.

- Si no hay ninguno, se posiciona respecto al `<html>` (viewport).


```css
.padre {
  position: relative;
}

.hijo {
  position: absolute;
  top: 0;
  right: 0;
}
```

---

### 4. `fixed`

- El elemento se posiciona **respecto al viewport** (pantalla).

- **No se mueve aunque se haga scroll**.

- Se usa para menús fijos, botones flotantes, etc.


```css
.fixed-menu {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
}
```

---

### 5. `sticky`

- Combina `relative` y `fixed`.

- Se comporta como `relative` **hasta que alcanza un punto fijo** al hacer scroll.

- Luego se comporta como `fixed`.


```css
.titulo {
  position: sticky;
  top: 0;
  background: white;
}
```

🔸 Ideal para cabeceras que **quedan pegadas** mientras se hace scroll.

---

## 📏 Propiedades relacionadas

- `top`, `right`, `bottom`, `left`: definen desplazamiento desde los bordes del contenedor de referencia.

- `z-index`: define el orden de apilamiento (qué elemento queda encima).


```css
.box {
  position: absolute;
  top: 0;
  right: 0;
  z-index: 10;
}
```

---

## 🧱 Ejemplo práctico

```html
<div class="contenedor">
  <div class="caja-relativa">
    Relativa
    <div class="caja-absoluta">Absoluta</div>
  </div>
</div>
```

```css
.contenedor {
  position: relative;
  width: 300px;
  height: 200px;
  background: lightgray;
}

.caja-relativa {
  position: relative;
  background: lightblue;
  padding: 20px;
}

.caja-absoluta {
  position: absolute;
  top: 0;
  right: 0;
  background: tomato;
  padding: 10px;
}
```

---

## ✅ Buenas prácticas

- Usa `relative` como **referencia** para `absolute`.

- No abuses de `fixed` y `absolute`, ya que pueden solapar o romper el diseño si no se controlan bien.

- Usa `sticky` para headers o elementos que deben quedar visibles al hacer scroll.

- Usa `z-index` solo cuando realmente necesites controlar capas.


---
