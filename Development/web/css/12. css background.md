La propiedad `background` se usa para aplicar **colores, imágenes o patrones** al fondo de un elemento HTML. Puedes controlar su color, posición, tamaño, repetición y comportamiento.

---

## 🎨 `background-color`

Define un color de fondo:

```css
body {
  background-color: #f5f5f5;
}
```

Admite:

- nombres (`red`)

- hexadecimal (`#ff0000`)

- RGB/RGBA (`rgb(255,0,0)`, `rgba(255,0,0,0.5)`)

- HSL (`hsl(0, 100%, 50%)`)


---

## 🖼️ `background-image`

Permite usar una imagen como fondo:

```css
body {
  background-image: url('fondo.jpg');
}
```

🔹 También puedes usar imágenes SVG, degradados o imágenes base64:

```css
background-image: linear-gradient(to right, red, blue);
```

---

## 🔁 `background-repeat`

Define cómo se repite una imagen:

```css
background-repeat: repeat;       /* valor por defecto */
background-repeat: no-repeat;    /* no se repite */
background-repeat: repeat-x;     /* solo horizontal */
background-repeat: repeat-y;     /* solo vertical */
```

---

## 🎯 `background-position`

Controla la posición de la imagen:

```css
background-position: top left;
background-position: center center;
background-position: 50% 50%;
```

---

## 🔍 `background-size`

Controla el **tamaño** de la imagen:

```css
background-size: auto;        /* por defecto */
background-size: 100px 200px; /* ancho y alto */
background-size: cover;       /* cubre todo el área sin distorsionar */
background-size: contain;     /* se ajusta sin recortar, puede dejar espacios */
```

---

## 📐 `background-attachment`

Define si la imagen se mueve con el scroll o queda fija:

```css
background-attachment: scroll;  /* por defecto */
background-attachment: fixed;   /* fondo fijo */
background-attachment: local;   /* se mueve con el scroll del contenido */
```

---

## 🧩 `background-clip`

Define hasta dónde se pinta el fondo:

```css
background-clip: border-box;   /* incluye border (por defecto) */
background-clip: padding-box;  /* hasta padding */
background-clip: content-box;  /* solo contenido */
```

---

## 🧱 `background-origin`

Controla desde dónde se posiciona la imagen de fondo:

```css
background-origin: padding-box;  /* por defecto */
background-origin: border-box;
background-origin: content-box;
```

---

## 🔄 `background` shorthand (atajo)

Puedes escribir todo en una sola línea:

```css
background: #f0f0f0 url('fondo.jpg') no-repeat center center / cover fixed;
```

El orden suele ser:

```
color image repeat position / size attachment
```

---

### 🛠️ `background-blend-mode`

La propiedad `background-blend-mode` define **cómo se mezclan varias capas de fondo** (`background`) entre sí, o con el color de fondo del elemento.

Es especialmente útil cuando combinas **imágenes y gradientes**, y quieres lograr efectos visuales similares a los de programas de edición como Photoshop.

**¿Cómo funciona?**

Cuando usas varias capas en `background` (como una imagen + un gradiente), el navegador
puede mezclar esas capas usando diferentes modos de fusión.

```css
background: linear-gradient(...), url(...);
background-blend-mode: multiply;
```

**Modos disponibles**

| Valor         | Descripción                                                             |
| ------------- | ----------------------------------------------------------------------- |
| `normal`      | No aplica mezcla, se apilan normalmente                                 |
| `multiply`    | Oscurece combinando colores (como si se superpusieran vidrios de color) |
| `screen`      | Aclara el resultado                                                     |
| `overlay`     | Mezcla oscureciendo/iluminando según el fondo                           |
| `darken`      | Muestra el color más oscuro                                             |
| `lighten`     | Muestra el color más claro                                              |
| `color-dodge` | Aumenta brillo del fondo                                                |
| `color-burn`  | Oscurece el fondo                                                       |
| `hard-light`  | Contraste fuerte según luminosidad                                      |
| `soft-light`  | Contraste suave                                                         |
| `difference`  | Resta el color del fondo y del frente                                   |
| `exclusion`   | Similar a `difference`, pero más suave                                  |
| `hue`         | Usa el matiz del frente y la luminosidad del fondo                      |
| `saturation`  | Mezcla usando saturación del frente                                     |
| `color`       | Usa color del frente con luminosidad del fondo                          |
| `luminosity`  | Usa luminosidad del frente con el color del fondo                       |

**Ejemplo**:

```css
.hero {
  background: linear-gradient(to right, rgba(255, 0, 0, 0.5), rgba(0, 0, 255, 0.5)),
              url('https://images.unsplash.com/photo-1507525428034-b723cf961d3e?auto=format&fit=crop&w=1400&q=80');
  background-blend-mode: overlay;
  background-size: cover;
  background-position: center;
  height: 100vh;
}
```

🔹 Este ejemplo combina una imagen con un degradado rojo–azul y usa `overlay` para mezclar los colores.

**importante**

- Funciona solo si tienes **más de una capa** de fondo.

- El orden de las capas en `background` **importa**.

- Puedes usar `background-blend-mode` junto con `mix-blend-mode`, que se aplica entre el fondo y otros elementos visuales, no solo capas.


---

## 🧪 Ejemplo completo

```css
.hero {
  background: linear-gradient(to right, #4a90e2, #9013fe), url('img/bg.jpg');
  background-blend-mode: multiply;
  background-repeat: no-repeat;
  background-position: center;
  background-size: cover;
  height: 100vh;
  color: white;
}
```

---

## ✅ Buenas prácticas

- Usa imágenes optimizadas para web (`.webp`, `.avif`, `.jpg`)

- Usa `background-size: cover;` para fondos de pantalla completa

- Usa `background-color` como **color de respaldo** si la imagen no carga

- Evita fondos pesados o en bucle innecesario que afecten el rendimiento

- Combina colores + imágenes + degradados con `background-blend-mode` si deseas efectos visuales avanzados


---
